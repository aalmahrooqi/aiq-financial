# SPDX-FileCopyrightText: Copyright (c) 2026, NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Public MCP-only workflow configuration.
#
# The FastMCP component owns the HTTP server and asynchronous submit/poll
# lifecycle, so this NAT configuration deliberately has no general.front_end
# and keeps AI-Q's separate async deep-research layer disabled.
#
# Runtime choices preserved from the reference MCP service:
# - workflow.enable_clarifier: false (stateless MCP has no multi-turn HITL)
# - workflow.enable_escalation: true (intent routing selects shallow or deep)
# - workflow.use_async_deep_research: false (MCP is the only async job layer)
# - workflow.checkpoint_db: required shared PostgreSQL via AIQ_CHECKPOINT_DB
#
# Public credentials:
# - NVIDIA_API_KEY for hosted NIM inference
# - TAVILY_API_KEY for web search

general:
  use_uvloop: true
  telemetry:
    logging:
      console:
        _type: console
        level: INFO

llms:
  nemotron_llm_intent:
    _type: nim
    model_name: nvidia/nemotron-3-super-120b-a12b
    base_url: "https://integrate.api.nvidia.com/v1"
    temperature: 0.5
    top_p: 0.9
    max_tokens: 4096
    num_retries: 5
    chat_template_kwargs:
      enable_thinking: true

  nemotron_super_llm:
    _type: nim
    model_name: nvidia/nemotron-3-super-120b-a12b
    base_url: "https://integrate.api.nvidia.com/v1"
    temperature: 0.7
    top_p: 0.7
    max_tokens: 65536
    num_retries: 5
    chat_template_kwargs:
      enable_thinking: true

  nemotron_ultra_llm:
    _type: nim
    model_name: nvidia/nemotron-3-ultra-550b-a55b
    base_url: "https://integrate.api.nvidia.com/v1"
    temperature: 0.7
    top_p: 0.7
    max_tokens: 65536
    num_retries: 5
    chat_template_kwargs:
      enable_thinking: true

functions:
  data_sources:
    _type: data_source_registry
    sources:
      - id: web_search
        name: "Web Search"
        description: "Search the public web for current information."
        tools:
          - web_search_tool
          - advanced_web_search_tool

  web_search_tool:
    _type: tavily_web_search
    max_results: 5
    max_content_length: 1000

  advanced_web_search_tool:
    _type: tavily_web_search
    max_results: 2
    advanced_search: true

  intent_classifier:
    _type: intent_classifier
    llm: nemotron_llm_intent

  # Defined for workflow compatibility but disabled at the workflow boundary.
  clarifier_agent:
    _type: clarifier_agent
    llm: nemotron_super_llm
    max_turns: 3
    log_response_max_chars: 2000
    verbose: true

  shallow_research_agent:
    _type: shallow_research_agent
    llm: nemotron_super_llm
    exclude_tools:
      - advanced_web_search_tool
    max_llm_turns: 10
    max_tool_iterations: 5

  deep_research_agent:
    _type: deep_research_agent
    enable_citation_verification: true
    orchestrator_llm: nemotron_ultra_llm
    source_router_llm: nemotron_ultra_llm
    researcher_llm: nemotron_ultra_llm
    planner_llm: nemotron_ultra_llm
    writer_llm: nemotron_super_llm
    exclude_tools:
      - web_search_tool

workflow:
  _type: chat_deepresearcher_agent
  enable_clarifier: false
  enable_escalation: true
  use_async_deep_research: false
  checkpoint_db: ${AIQ_CHECKPOINT_DB}
